const { User, userValidation } = require('../schemas/user');
const { Article } = require('../schemas/article');
const { Partner } = require('../schemas/partner');

module.exports = { User, userValidation, Article, Partner };
